import 'package:flutter/material.dart';

class HeartSpecialistScreen extends StatelessWidget {
  const HeartSpecialistScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
